<%= form_for @schedule, :html => { :class => "form-horizontal schedule" } do |f| %>

  <% if @schedule.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @schedule.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :imported_at, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :imported_at, :class => 'form-control' %>
    </div>
    <%=f.error_span(:imported_at) %>
  </div>
  <div class="form-group">
    <%= f.label :person, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :person, :class => 'form-control' %>
    </div>
    <%=f.error_span(:person) %>
  </div>
  <div class="form-group">
    <%= f.label :day, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :day, :class => 'form-control' %>
    </div>
    <%=f.error_span(:day) %>
  </div>
  <div class="form-group">
    <%= f.label :allocation, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :allocation, :class => 'form-control' %>
    </div>
    <%=f.error_span(:allocation) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                schedules_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
